# V3 20260612 Pre-New-Plan Runtime Messages Supersession And Rebuild Policy

Gate: `V3_20260612_PRE_NEW_PLAN_RUNTIME_MESSAGES_SUPERSESSION_AND_REBUILD_POLICY_GATE`
Layer role: `runtime_control`
Result: `POLICY_PASS`
Mode: read-only audit / policy only

This gate did not execute rollback, write the database, start or modify a scheduler, manually execute wrapper/N3/N4/N5, consume or update outbox/inbox/checkpoint, enter N6, or touch voice/mobile/sim/position/PnL/real trade.

## Current Finding

Do not delete everything.

The safe policy is:

1. Preserve raw/source facts.
2. Supersede or rollback scoped derived old runtime messages in reverse dependency order.
3. Rebuild 20260612 from the new V3 realtime virtual metric plan.

Direct cleanup is not authorized yet because `com.ashare-v3.n3.intraday-b1-c1-b2-auto-poll` is currently loaded. It is not running between passes and latest report is `NOOP_PASS`, but any cleanup execute should first stop/pause the automatic scheduler.

## Live Inventory

Runtime DB proof:

```text
database = ashare_v3
db_user = chuanfuchen
db_now = 2026-06-12 19:31:25.687904+08
```

Scheduler proof:

```text
label = com.ashare-v3.n3.intraday-b1-c1-b2-auto-poll
state = loaded / not running between passes
StartInterval = 60
observed runs = 247
last exit code = 0
latest chain report = NOOP_PASS
```

## N3 Inventory

N3 market data run summary for `for_trade_date=20260612`:

| Status | Generated By | Runs |
|---|---:|---:|
| passed | market_data_layer | 2 |
| passed | N3-B1-realtime-snapshot-execute | 25 |
| passed | N3-B2-realtime-projection-execute | 16 |
| passed | N3-C1-today-minute-execute | 15 |
| running | N3-B1-realtime-snapshot-execute | 1 |

Preserve:

- N3 subscription / pull-plan lineage.
- Previous-day minute preload.
- C1 today `stock/index/board_minute_bar_1m` raw/source facts.
- B1 fact-only realtime snapshots unless a separate contamination audit proves they are invalid.
- N1/N2 source facts and condition/context rows.

Latest C1 raw/source minute fact example:

```text
today_minute_bar_1m_20260612_until_1500__...
stock/index/board = 58555/7887/4541
```

Supersede as old derived event-source / metric lineage:

- B1 standard outbox `MarketSnapshotUpdated` runs:
  `1107 / 1113 / 1120 / 1307 / 1314 / 1333 / 1413 / 1430 / 1444 / 1452 / 1500`
- Each standard outbox run has `MarketSnapshotUpdated pending=2082`.
- Trace-aligned B2 projection runs used by old N4 production replay:
  `1413 / 1444 / 1452 / 1500`, each `stock/index/board=1872/83/127`.

N3 source outbox status remains `pending`; N4 consumed these events through N4 inbox/checkpoint, but N3 outbox status was not updated.

## N4 Inventory

N4 production semantic replay runs to supersede:

| Run | State | Match | Outbox |
|---|---:|---:|---|
| `n4_production_semantic_replay_20260612_market_snapshot_updated_until_1413` | 1216 | 812 | TriggerMatched 812, TriggerPendingMarketData 404 |
| `n4_production_semantic_replay_20260612_market_snapshot_updated_until_1444` | 1179 | 775 | TriggerMatched 775, TriggerPendingMarketData 404 |
| `n4_production_semantic_replay_20260612_market_snapshot_updated_until_1452` | 1225 | 821 | TriggerMatched 821, TriggerPendingMarketData 404 |
| `n4_production_semantic_replay_20260612_market_snapshot_updated_until_1500` | 1245 | 841 | TriggerMatched 841, TriggerPendingMarketData 404 |

Boundary flags for these N4 runs are clean:

```text
action_layer_touched=false
user_layer_touched=false
voice_touched=false
sim_touched=false
real_trade_touched=false
worker_started=false
```

N4 inbox refs from N3 standard outbox:

```text
1413 / 1444 / 1452 / 1500 each processed 2082 MarketSnapshotUpdated rows
```

## N5 Inventory

N5 bounded action runs to supersede:

| Run | Source N4 Run | Action Facts | Outbox |
|---|---|---:|---|
| `n5_action_bounded_20260612_from_n4_production_semantic_replay_20260612_market_snapshot_updated_until_1444` | `...until_1444` | 775 | ActionBlocked 775 |
| `n5_action_bounded_20260612_from_n4_production_semantic_replay_20260612_market_snapshot_updated_until_1452` | `...until_1452` | 821 | ActionBlocked 821 |
| `n5_action_bounded_20260612_from_n4_production_semantic_replay_20260612_market_snapshot_updated_until_1500` | `...until_1500` | 841 | ActionBlocked 841 |

N5 consumed only N4 `TriggerMatched`:

```text
1444 = 775
1452 = 821
1500 = 841
```

Boundary flags for these N5 runs are clean:

```text
user_layer_touched=false
voice_touched=false
sim_touched=false
real_trade_touched=false
worker_started=false
```

## N6 / User / Sim Inventory

No N6/user/sim downstream refs were observed for the 20260612 N5 runs:

```text
user_projection_run = 0
user_signal_projection = 0
user_signal_card = 0
user_notification_queue = 0
n6_virtual_order/trade/position/position_event/pnl_snapshot = 0
user_sim_order/trade/position = 0
```

That means cleanup can start at N5 after the scheduler is stopped and live refs are refreshed again.

## Preserve Policy

Preserve:

- N1 official daily and condition source facts.
- N2 condition rows and trigger context source rows.
- N3 subscription and pull-plan lineage.
- N3 previous-day preload facts.
- N3 today minute 1m source facts.
- N3 B1 fact-only realtime snapshot source rows unless a separate contamination audit says otherwise.
- Historical execute reports as audit evidence.

Do not promote as active inputs after the new plan:

- Pre-new-plan B1 standard outbox runs.
- Pre-new-plan trace-aligned B2 projection runs.
- Pre-new-plan N4 production semantic replay runs.
- Pre-new-plan N5 bounded action runs.
- Any N6 projection rows if a refreshed final gate later finds them.

## Rollback / Supersession Order

Required order:

```text
0. stop/pause scheduler
1. N6/user refs, if any
2. N5 action facts/events/outbox/inbox/checkpoint
3. N4 trigger state/match/outbox/inbox/checkpoint
4. N3 derived standard outbox + trace-aligned B2 projection
5. runtime_control post-cleanup registration
6. new-plan rebuild
```

Do not clean N3 first. N4 has inbox/checkpoint refs to N3, and N5 has inbox/checkpoint refs to N4.

## Rollback SQL Requirements

Any cleanup SQL must:

- Hard-fail before the first `DELETE` / `UPDATE`.
- Require an explicit session flag.
- Use only scoped run_id/event_id allowlists.
- Guard scheduler state or require a prior stop gate.
- Guard wrapper/child process count.
- Guard N6/user/sim/virtual refs before N5 cleanup.
- Guard N5 refs before N4 cleanup.
- Guard N4 refs before N3 derived cleanup.
- Deny `DROP`, `TRUNCATE`, and `CASCADE`.
- Deny N3 raw source fact deletion unless separately approved.
- Deny N1/N2 source row deletion.

Must not delete:

- `common_trade_calendar`
- N1 source facts
- N2 condition rows
- market-data subscription / pull-plan rows
- previous-day minute preload rows
- today minute source facts
- fact-only B1 snapshot source rows

## Rebuild Route

After scoped cleanup:

1. Run the V3 realtime virtual metric schema/migration final gate if production DB schema is not yet migrated.
2. Generate N3 realtime virtual metrics for 20260612 from preserved raw 1m K and snapshot facts.
3. Emit the new standard metric event/fact lineage for N4.
4. Run N4 production trigger using only N3 standardized realtime virtual metrics.
5. Run N5 bounded action from `TriggerMatched` only.
6. Keep N6/voice/mobile/sim/trade out of scope unless a separate N6 gate is explicitly approved.
7. Generate `V3_20260612_NEW_PLAN_REBUILD_CLOSEOUT.md/json` and compare diagnostics with target-machine `B_BUY=76 / S_SELL=24`.

## Decision

`POLICY_PASS`.

Direct cleanup final gate is **not** allowed yet because the scheduler is loaded.

Allowed next gate:

```text
V3_20260612_PRE_NEW_PLAN_RUNTIME_MESSAGES_SCHEDULER_STOP_OR_PAUSE_GATE
```

After scheduler stop/pause and a refreshed live ref scan, proceed to:

```text
V3_20260612_PRE_NEW_PLAN_RUNTIME_MESSAGES_CLEANUP_CONTRACT_PREFLIGHT_GATE
```

## Next Prompt

```text
layer_role=runtime_control。

进入 V3_20260612_PRE_NEW_PLAN_RUNTIME_MESSAGES_SCHEDULER_STOP_OR_PAUSE_GATE。

目标：在不执行 rollback、不写数据库、不手动执行 wrapper/N3/N4/N5/N6、不消费/update outbox/inbox/checkpoint、不触碰 voice/mobile/sim/trade 的前提下，scoped 停用 com.ashare-v3.n3.intraday-b1-c1-b2-auto-poll，为 20260612 pre-new-plan cleanup final gate 做安全前置。停用后只读复核 scheduler not_loaded、wrapper/child process count=0、20260612 N3/N4/N5/N6 refs 未新增，然后进入 cleanup contract/preflight gate。
```
